== commands task: 

1- git add *

2- git remote add deploy https://git.heroku.com/example-app.git

3-git log --oneline

4- git config --global user.name "Yousab"
   git config --global user.email "yousabnasrala@gmail.com"

5- git switch development
   git merge feature/chat

6- git commit --amend

== what is head?
  head is a pointer to you current commit object from which any modifications will be
  added as new commits on its branch

== problems with commit:
  1- message head is not meaningful and does not summiries what the changes are
  2- message body describes a lot of changes ,bug fixes and refactoring but doesnt state any of them
  3- "not sure exacly what i did" 
  4- "Hope this doesn't break anything else."
  5- adding random changes to config without spicifing them
  6- pushing ai changes without fully testing them 

==which is not true:
    "Git is a cloud-based service to store remote repositories."
  git is not a cloud service is a version control system that allows you to track 
  changes in repositories
  those repos can be hosted on cloud services like github

==my repositorie : https://github.com/yousab21/Git-with-IEEE-computer-Society

==my pull request : https://github.com/cat-backend-nodejs/i-can-pull-it/pull/100
